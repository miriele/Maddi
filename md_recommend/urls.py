from django.urls.conf import path
from md_recommend import views

app_name = "md_recommend"

urlpatterns = [
    # path("Aaaa",       views.AaaaView.as_view(),       name="Aaaa"),    # view 추가하면 삭제하세요
    ]